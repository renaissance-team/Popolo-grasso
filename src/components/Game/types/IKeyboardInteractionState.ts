export interface IKeyboardInteractionState {
  arrowLeft: {
    pressed: boolean;
  };
  arrowUp: {
    pressed: boolean;
  };
  arrowRight: {
    pressed: boolean;
  };
  arrowDown: {
    pressed: boolean;
  };
}
